mutation Login($data: AuthInput!) {
  login(data: $data) {
    user {
      id
      email
      role
      isEmailVerified
      profile {
        fullName
        age
        gender
        bio
      }
    }
  }
}
