mutation Register($data: AuthInput!) {
  register(data: $data) {
    user {
      id
      email
      role
      isEmailVerified
      profile {
        fullName
        age
        gender
        bio
      }
    }
  }
}
